"""
agent — LangGraph-based Agentic RAG orchestration.

Replaces the brittle regex-based query routing with an LLM-driven
query analyzer that understands intent, extracts entities, and
routes to the appropriate data sources.
"""
